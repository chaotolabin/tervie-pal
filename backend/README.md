Sửa lại sau, note các bước chạy app.main đã
1. `cd .\backend\`
2. `python -m venv venv`
3. `.\venv\Scripts\activate`
4. `pip install -r requirements.txt`
5. `fastapi dev app/main.py` hay `uvicorn app.main:app --reload`